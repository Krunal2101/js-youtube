
let index = 0
while (index <= 10) {
    // console.log(`index num is ${index}` );
    index = index + 2
}

let myarray = ['krunal', 'himay', 'milan']

let arr = 0
while (arr < myarray.length) {
    //console.log(`num of array ${myarray[arr]}`);
    arr = arr +1
}

// do while

let score = 1
do {
    console.log(`num of score ${score}`);
    score = score+1
} while (score<= 10);